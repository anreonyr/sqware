[package]
name = "table"
version.workspace = true
edition.workspace = true
description = "无堆表格渲染：行导向有界格子 + 自研逐行渲染（&mut fmt::Write 出口，无 alloc）"

[lib]
test = false
bench = false
doctest = false

[dependencies]
arrayvec = { version = "0.7.8", default-features = false }
