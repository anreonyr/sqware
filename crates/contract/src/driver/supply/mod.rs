//! supply — **物料到手**里**已搬进「约」的两件**：据（`core`）与形（`frame`）。
//!
//! 客侧（`client`）**还留在 `protocol`**：它吃一枚 `&Pier`，而 `Pier` 要碰内核——等签名关。
//! 正文见 `protocol` 那一侧的 `driver/supply/mod.rs`。

pub mod core;
pub mod frame;
