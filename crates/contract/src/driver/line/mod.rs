//! line — **线**里**已搬进「约」的两件**：据（`core`）与形（`frame`）。
//!
//! **照实记（客侧留在口那一侧）**：`line` 从前整族卡在 `crate::session::Pier` 上（那个类型当时
//! 住在口侧、且它的推/收直接叫内核）。`Pier` 手表化（刀二 a）之后据只用一枚注入的 `&Pier`，
//! 故据与形过来了；**客侧（`client`）还留着**——它自己铸孔、自己 `claim`，碰的是内核，
//! 判据把它划在「口」。正文见 `protocol` 那一侧的 `driver/line/mod.rs`。

pub mod core;
pub mod frame;
