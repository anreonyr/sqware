//! driver — **设备轴**里**已搬进「约」的那几件**（形与据）。
//!
//! **照实记（这一轴只搬了一半）**：`supply`（物料到手）的形与据是纯的，搬了；`line`（线）这一半
//! **还留在 `protocol` 那一侧**——它的据写着 `crate::session::Pier`，而 `Pier` 的推/收要碰内核
//! （靶里那份 224 行替身就是它的影子）。「碰内核的那一手怎么从约里划出去」是**签名关**的事，
//! 那一关过了，`line` 这一半连同 `supply/client.rs` 一起归位。正文（`driver/mod.rs` 那一份）
//! 也留在 `protocol`，等它那一族搬齐再过来。

pub mod supply;
