//! Operator Protocol — **命名寻址**：一棵树，名字 → Pie。
//!
//! 全系统**就一个** Operator，管着所有条目；其他任务只是**操作**它：
//!
//! ```text
//!   part  分   分出一块空窗格（Pane）
//!   land  落   把一枚 Pie 落到一个名字上（贴一枚 Tile）
//!   find  寻   走到头，把那一枚 Pie 交出去
//!   trim  剪   剪掉一条
//!   list  列   看一块 Pane 里有哪些**号**
//!   name  名   这枚号此刻叫什么
//!   seek  译   把一条路**译成号**——名字只到这一格，往下一律按号
//!   opens 开   第 n 格是谁的门牌（**只读**，不上线：持树者判 `Rule::Opens` 时自己问）
//! ```
//!
//! 对外那七条都是 4 个字母；第八条是 5 个字母、只读、不发消息——照实记：这一族的字是**语义
//! 名词/动词**（`part`/`land`/`find`/`trim`/`list`/`seek`/`name`），故 `peek`/`probe`/`look`
//! 那类实现细节词一个都没用上。
//!
//! 两个类型名与两条原语名**不同名**（`part` 建 `Pane`、`land` 落 `Tile`）——这是照实记：名动同形
//! 那一格本来留给 `tile`，最后定的是 `land`（"落位"那个画面），而 `seat` 因为与会话的
//! `Seat` / `Quay::seat` 撞在同一个文件里而被否掉。
//!
//! # 树
//!
//! ```text
//!   slots: Vec<Option<Slot>>      号 i ↔ slots[i]；None = 墓碑（剪掉 / 剔死留下的坑）
//!   Slot  { 名字, 去处 }          名字 = 一段
//!   Node  = Pane(Vec<EntryId>)    窗格：装的是**孩子的号**，还能往里走（可以再分）
//!         | Tile(PieToken)        砖：到头了，就是内核那一枚
//!   根    = 顶层那个 Vec<EntryId> `Where::Root` 就是它（**根没有号**）
//! ```
//!
//! **号就是下标**（照实记：原来是一棵嵌套树，按深度递归 ⇒ 持树者死在第 117 层）。
//!
//! 一条路是**段列表**（`&[Name]`），不是一个字符串：一段就是现成的 `Name`（定长 32 字节、
//! 构造即校验）——于是"名字不合法"在类型上不存在，也没有分隔符 / 转义 / `..` 这些边界。
//! **路只有一处用处**：[`Operator::seek`] 把它译成号（见下一节）。
//!
//! # 名与号分开
//!
//! 机器用**号**（[`EntryId`]），人用**名**（`Name`）。号在 `part` / `land` 时铸、**换绑不动号**、
//! `trim` 与 `find` 剔死则号失效（水位只上不下 ⇒ 号不重用）；**根没有号**，故 0 是第一个真格子。
//!
//! 名字是**间接**的坐标：一条路（段列表）全系统只有一处用处——[`Operator::seek`] 把路**译成号**。
//! 译完就按号走：`find` / `trim` / `name` 收**号**，`land` / `part` / `list` 收**容器坐标**
//! [`Where`]（根，或某一号）——根没有号，故它显式占 [`Where::Root`] 那一格。名字在答话那一侧
//! 也留着一条（[`Operator::name`] 按号答名），于是"人念得出的那半"与"机器认得的那半"各有
//! 一条路，中间只有 `seek` 这一道闸。
//!
//! # 七条原语（对外）+ 一条只读（不上线）
//!
//! | 原语 | 收什么 | 干什么 | 答什么 |
//! |---|---|---|---|
//! | [`Operator::part`] | 容器坐标 + 新名 | 保证那一格是一块 `Pane`（**幂等**：已经在就是成了） | **那一格自己的号** |
//! | [`Operator::land`] | 容器坐标 + 新名 + 一枚 Pie | 贴一枚 `Tile`：空着就落上，占着就是换绑 | **那一格自己的号** |
//! | [`Operator::find`] | 号 | 把那一号后面那一枚 Pie 交出去 | 一格状态 |
//! | [`Operator::trim`] | 号 | 把那一号剪掉 | 一格状态 |
//! | [`Operator::list`] | 容器坐标 | 读那一块 `Pane` 里的**号** | 一串号 |
//! | [`Operator::seek`] | 一条路 | 把路**译成那一枚号**（名字只到这一格） | 一枚号 |
//! | [`Operator::name`] | 号 | 把一枚号翻成名字 | 一枚名字 |
//!
//! **第八条 [`Operator::opens`]（开）不发消息**：它答"第 `n` 格是谁的门牌"，是持树者自己判
//! `Rule::Opens` 时问的一句（**没有动作码**）。与 `find` 正相反——`find` 交出句柄，它只答
//! "是谁"，不动树、不交东西。也**不分配**（同 `name` 那一档）。
//!
//! **要动一块 `Pane`，先把它清空**——`land`（换绑）与 `trim` 是同一条规矩：
//! 非空 `Pane` ⇒ [`Fail::NonEmpty`]。
//!
//! # `land` 那一问是这一格的**整值赋值**（庚：第一关裁的）
//!
//! 这一格的值是**三元组**：**那一枚**（`Tile` 里那枚 Pie）＋ **用**那一轴（`Rule`：谁许用）
//! ＋ **改**那一轴（`mine`：归不归落牌的那一位）。线上那一问带的就是这一整份，持树者照单全收、
//! 一次写完（[`Ledger::write`](ledger::Ledger::write)）。
//!
//! **核心那一侧只有一件**：[`Operator::land`] 贴一枚 `Tile`——空着就铸号、占着就换绑，两条
//! 是 `put` 里同一支；两轴不进核心（核心连 `Rule` 都不认识）。故"一条原语担四件事"这句的
//! 准确形状是：**担四件的是 `LAND` 那一问，不是 `land` 那一条原语**。
//!
//! **五条来路**——同一句问在五种前置状态下的五个名字：
//!
//! | 来路 | 那一刻那一格 | 与默认不同的那一格 | 今天谁走过 |
//! |---|---|---|---|
//! | 落牌 | 空着 | — | 六台生产域 + 四台测具 |
//! | 换绑 | 一枚砖（或一块空窗格） | `entry` 换了 | `probe-rule` 换绑那一手、`probe-owner` 的接手 |
//! | 改规矩 | 一枚砖 | `rule` 换了 | **0 处** |
//! | 放弃归属 | 一枚砖、主人是我 | `mine = false` | **0 处** |
//! | 认领归属 | 一枚砖 | `mine = true` | `uart`（生产里唯一一处） |
//!
//! **`land` 从不比对**新旧那一枚是不是同一个东西（已占那一支只把新值写进那一格、再放下旧的）
//! ⇒ 线上"换绑"与"只改规矩"**逐字相同**：两条的区别只存在于发起者心里。
//!
//! **没有偏更新**（只改一轴、不动那一枚）。上表里"改规矩"与"放弃归属"没有独立入口：唯一写法是
//! **重 `LAND` 一次**（同坐标 + 同名 + 同一枚），而那一趟连着三笔——
//!
//! 1. **要再交一次句柄**（`client::land`(client::land) 头一手就是 `ship`，
//!    造一份新副本种进持树者表里）；
//! 2. **树那一格被重写**（不是"只改账"）：新副本与旧副本**同时在场过**（先 `ship`、后放下）
//!    ⇒ 那一格指着的**不再是原来那一枚**；而**格号（[`EntryId`]）不动**——两个"号"是两件事；
//! 3. **手里没有那一枚就改不了**：`ship` 的源头是自己手里那一枚，而内核 `Accord` 那道闸是
//!    "持 `VEST` 才交得出去"；唯一绕开的路是**原主人已死**
//!    （[`Ledger::claimable`](ledger::Ledger::claimable) 的接手支）——那时谁都能重落，
//!    但那已经不是"改规矩"，是**换人**。
//!
//! **照实记（庚 的翻案条件）**：偏更新要单开一条原语的话，代价是动作码 + 帧形 + 门禁落点 +
//! 宿主靶 + 读数表全动；而它在线上**照样是 upsert**（账上没有那一行时是插入——`part` 分出来的
//! 窗格本来就没有行 ⇒ 照样得先要位），"比 `land` 简单"是假的；且它的地址只有 (坐标, 名)，
//! **"那一格在不在"没有判据**（账是缓存，树才是真相）。⇒ 翻案要**一位真客人**说出一句原话
//! （"把这一格的规矩换成 X，**不要动那一枚**"）并给得出理由。**今天零位。**
//!
//! **这一格说不出的操作**（读回是"改"的对偶，另四条各有各的来路）：
//!
//! | 说不出 | 为什么 |
//! |---|---|
//! | **读回**规矩 / 归属 | 两轴**只写不读**：`Ledger::rule` 只在 `find` 门口被内部问一次，线上四种答形**没有一个**带规矩 |
//! | 给**窗格**立规矩 | `part` 不写账、`land` 只落砖 ⇒ **规矩只存在于砖那一格**；一块有孩子的窗格永远带不上（`land` 先撞 [`Fail::NonEmpty`]） |
//! | **改名** | 换个名 = 落一格新的，格号也换 |
//! | **转交归属**给另一位 | 只能"放弃 + 对方接手" |
//! | **撤销**已授出的那一枚 | 见上面"交出去的权柄收不回来"那一节 |
//!
//! # 失败域只有六格
//!
//! [`Fail`]：`Unknown` / `NonEmpty` / `NotATile` / `NotAPane` / `Full` / `Dead`——每格对应一个
//! **不同的下一步**。后两格的"不是谁"跟着两个类型名走：寻到头是**窗格** ⇒ `NotATile`
//! （它不是一枚 `Tile`）；要往里走 / 要列，可那一段是**砖** ⇒ `NotAPane`。
//!
//! **没有"名字已被占"那一格**：同名接手一枚 `Tile`、或一块**空的** `Pane`，都是换绑；而 owner
//! 归 Principal，Operator 分不出"自己 / 别人"，"已占即拒"在这里无处落脚。
//!
//! # 签名里没有"谁"
//!
//! 那八条原语都没有 caller 参数、**树上的条目**也没有 owner 字段：**谁能动由 Principal 那一层
//! 回答**（内核在 Push 时盖的发送者印章是现成的，Principal 拿去用）。
//!
//! **照实记（"Operator 不看它"那一句是错的）**：那一枚章要看，看的是两处**与身份无关**的事——
//! 认这一趟的回信孔（`opened_by == 发送者`）与账上**改**那一轴的主人（`Line` 的 `owner.who`，
//! `land` / `trim` 拿它判）。"这一位此刻代表哪条号"始终归 Principal。
//!
//! # 两枚注入的戳子 + 一次注入的动作
//!
//! [`Stamps`]（那一枚 Pie 还答得出吗 / 这扇门是谁开的）与 [`Unship`]（把我这一份放下——剪掉或
//! 换掉一枚 `Tile` 时用它，不加这一格那一枚句柄就漏在树里）。核心因此不 `use` 内核，喂三个假
//! 闭包就能把规矩推理干净。两枚戳子收在**一格具名的组**里：它们同型，摆成位置参数时写反了
//! 编不过（`session` 那一族认 `owner` 还是 `grantor` 实测栽过一次）。
//!
//! # 与 [`system::board`](crate::system::board) 的关系
//!
//! `board` 是**另一件事**：一块公示板 + 一本待客台账（名字可以立着而没有东西、记得"谁挂的"、
//! 有死亡道、摘牌子不流转）。这一份是**一棵命名树**：结构长在它自己里面、不记 owner、
//! 谁问都答。两处并存，去留不在本正文里定。
//!
//! # 落地程度
//!
//! 三层都在：**核心**（[`core`]：树 + 八条原语；用例在**编外宿主台**
//! `protocol-case` 的 `operator` 靶，门口 `crates/gate/tests/host.rs`）、**载体**（`call` 的帧与转发、
//! 持树者那本客人小账 `desk`）、**服务**（[`server`](/crate::system::operator::server) 的
//! `serve` / `attach` / 客侧三手，加 `prog-operator` 这个域；装配那一格在
//! `programs/.../service.rs` 的 `Program::operator`）。
//!
//! **七条都在线上**（第八条 `opens` 不上线）：两条一格状态的（`find` / `trim`）与**五条答数据的**
//! （`land` / `part` 各答一枚号、`seek` 答一枚号、`list` 答一串号、`name` 答一枚名字）。
//! **答话那一侧有四种帧形**（一格状态 / 一串号 / 一枚名字 / 一枚号），各有各的上界
//! （见 `call::REP_LEN` 那一段）；`list` / `name` / `seek` 三家分家，就是"名与号分开"那一条
//! 落地：**名字只到 `seek` 这一格**。
//!
//! **问话那一侧按动作各一条帧形**（`seek` 带路，`land` / `part` 带容器坐标 + 新名，
//! `list` 带容器坐标，`find` / `trim` / `name` 带号）；最长的仍是 `seek` 那一条
//! （`call::REQ_LEN`），其余都落在十到五十字节。
//!
//! # 交出去的权柄收不回来（已知边界，照实写不是待办）
//!
//! [`Operator::find`] 交出去的那一枚**带 `VEST`**（`call` 那一份记了为什么：持树者查到名字时
//! 要能**再授出**）⇒ 拿到它的人可以再传、可以再落；而**改这一格的规矩影响不到它**：`Rule`
//! 判的是"许不许往外授"那一手，不是"它拿在手里能干什么"。
//!
//! **本仓没有任何一族协议带"撤销"这一格**——载体有（`mail::revoke` 能收回我授给某位的副本、
//! 含其全部后代），协议层没有；仓里唯一写过这一句的是
//! [`driver::supply`](crate::driver::supply)（"**没有撤销**：授出去的门闩归对端；要收回得走
//! `Revoke`，本协议不带这一格"）。要收，先得裁两件事：**谁来撤**（"这一格归谁改"那一轴已经有
//! 主人，而"撤回已经授出去的"是另一件事）、**撤到什么程度**（那一枚的下游要不要一并失效）。
//! 这一格今天停在"**载体做得到、协议没这一格**"。


pub mod core;
pub mod frame;
pub mod gate;
pub mod judge;
pub mod ledger;

pub use crate::system::operator::core::{EntryId, Fail, OpenedBy, Operator, Stamps, Unship, VestedBy, Where};
