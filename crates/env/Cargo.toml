[package]
name = "env"
version.workspace = true
edition.workspace = true

[lib]
test = false
bench = false
doctest = false

[dependencies]
bitflags = "2"
erra = { version = "0.2.0", default-features = false }
envmacros = { path = "../envmacros" }
