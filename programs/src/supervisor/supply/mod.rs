//! supply::实现侧 — **引导域那一侧**：照单取源、授出、回一张回单。
//!
//! 固件面的**规范与接口**（正文、失败域、帧与词表、编排域怎么去领）住
//! `crates/protocol/src/driver/supply/`；**实现方**（引导域的那圈发货循环）住这里——
//! **位置跟域走**（root 是引导域，不是驱动）、**名字跟协议走**。

pub mod server;
