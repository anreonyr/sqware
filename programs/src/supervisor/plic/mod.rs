//! plic::实现侧 — **中断面域**：程序本体、设备面，与它自己那片硬件账。
//!
//! `main.rs` 是它的入口（bin），`plic.rs` / `uart.rs` 是它的设备模块（也由那份 bin 自己
//! `mod` 声明——**同一份源码不编两遍**）。本模块只把 [`needs`] 交给 lib：那张需求单是**收方
//! 自己开的**，装配者只是照它开单，故两边必须看同一张表。

pub mod needs;
