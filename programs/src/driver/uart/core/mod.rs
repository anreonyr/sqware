//! uart::core — **纯功能**：不碰内核、不碰设备的那一半。
//!
//! ```text
//!   batch.rs   交出去的那一批（非空不可表达）
//! ```
//!
//! 本域的服务面**没有帧、也没有"那一格"**（它是一条字节流；"一条消息 = 一次排空"，见
//! `driver/uart/mod.rs`）——故这一层只有"这一批能不能交"这一条纪律：没有 `frame.rs`、
//! 也没有会话核。碰内核与设备的那几手住 `adapt/`，设备面住 `uart.rs`。

pub mod batch;
