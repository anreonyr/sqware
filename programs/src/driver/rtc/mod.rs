//! rtc::实现侧 — **第二台设备驱动**：`rtc@101000` 的持有者（11 号线）。
//!
//! `main.rs` 是它的入口（bin），`rtc.rs` 是它的设备面（也由那份 bin 自己 `mod` 声明——同一份
//! 源码不编两遍）。本模块只把 [`needs`] 交给 lib：那张需求单是**收方自己开的**，装配者只是照
//! 它开单，故两边必须看同一张表（与 `driver/uart` 同一个形状）。

pub mod needs;
