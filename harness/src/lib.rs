#![no_std]
//! harness — **测具**那一档：**不进产品镜像的一切**——探针（只读数）· 试客（量服务）·
//! 压测台（整台替换引导镜像）。
//!
//! # 这一档与产品那一档的界线
//!
//! **产品**（隔壁那个 crate `programs`）：内核起的那套服务 + 真客人。
//! **测具**（本 crate）：**只为了量**而存在的程序——
//!
//! - **探针**（`probe-*.rs`，5 份）：进**验收镜像**当客人跑。`soak` / `examine` 的判据
//!   就是它们打出来的那几行；故它们**必须**在验收镜像的清单里（这是"测具出产品镜像"这件事
//!   唯一做不到的一格）。
//! - **试客**（`guest` / `passer` / `lodger` / `sleeper` / `subject` / `member`，6 份）：**进验收
//!   镜像当客人跑**，量的是**服务**（身份 / 盟籍 / 时钟 / 树 / 生死账）。它们原先住
//!   `programs/src/user/`（与产品程序同住一间）——**用户裁定"测试不许住在运行时源里"**那一刀
//!   把它们也搬了过来，见下面那条照实记。
//! - **压测台**（`rig` / `load` / `beat` / `again` / `group`，与它们的受害者 `churn` / `hang` /
//!   `park` / `busy` / `waiter`，10 份）：**整台替换引导镜像**，与验收场景没有交集。
//!
//! 本 crate 只借产品那一件共享入口：每个程序一声 `extern crate programs;` ⇒ `programs::entry`
//! 的 `_start`（`global_asm!`）与 panic 处理被拉进链接；链接脚本也是同一张
//! （`programs/link.ld`，镜像都链在 `IMAGE_BASE = 0x10000`）。
//!
//! **特权级与"哪几台进哪张镜像"只在 `plan::assembly::ALL` 声明**（与产品同一条规矩）：前者是
//! 每行的 `kind`，后者是每行的 `scenes`。验收那张列 **9** 份产品 + **5** 份探针 + **6** 位试客
//! （共 20 条）；
//! 台子那张只列台主与它要的受害者。
//!
//! # 照实记（为什么搬家）
//!
//! 这两族原先与 15 份产品同住 `programs/`：同一个 crate、同一张 `[[bin]]` 表、同一张**并集**
//! 那张清单（并集）⇒ 跑默认（验收）镜像也把 10 台压测台装进去，而那 10 台在那张镜像里
//! **永远不会被起**（≈1.6 MB / 5.6 MB）。用户的话是
//! **"为什么 programs 里面的测试和程序混在一起？我不希望这样"** ⇒ 测具整体搬到这里，
//! 清单同时拆成按场景的几张表。
//!
//! **照实记（先验的那一格）**：`extern crate programs;` 把 `entry.rs` 的 `_start` 带过 **rlib
//! 边界**这件事，是这一刀成立的前提——先拿一份探针试过（`nm` 里 `T _start` 在、ELF 正常），
//! 才动的其余的。验不过的退路是"同 crate 分目录"。
//!
//! **照实记（第二刀：试客也搬来了）**：第一刀（用户裁定"为什么 programs 里面的测试和程序混在
//! 一起"）搬的是探针与压测台，六位试客留在 `programs/src/user/` 与产品程序同住一间——而它们
//! 早就算本 crate 的定义（**只为了量**；去掉机器照转，产品镜像实测过）。这一刀按用户裁定
//! **甲** 把它们搬过来，边界这才变成一句话：**`programs/` = 产品镜像那 9 条；本 crate =
//! 不进产品镜像的一切**（探针 5 + 试客 6 + 压测台 10 = **21**）。

extern crate alloc;

/// **用例与运行器**（程序侧那一台）——用户裁定"服务台搬进 SUT"那一刀之后，它搬去了**自己
/// 一个 crate**（`crates/cases`）：`harness` 依赖 `programs`，而十台服务域住在 `programs` 里
/// ⇒ 两边都要用的东西不能住在本 crate（会成环）。这里只**把它转出去**，故探针那 5 份的
/// `use harness::cases;` 一个字不用改（那份文件的头注也跟着搬过去了）。
pub use cases;

/// 压测台共用的**测时与空转**（各台互不依赖，共享的只有这一份"怎么量时间"）。
pub mod tick;

/// 房客那一片模块：`lodger` 的 bin 要从 lib 拿它自己那张 [需求单](lodger::needs)——
/// 与 `driver/*/needs.rs` 同一个形状（**收方开的单子**，装配者只是照它开单，故两边必须看同一张表）。
pub mod lodger;
