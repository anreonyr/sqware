/target
trace/

# 编辑器状态归本地：`docs/` 是一个 Obsidian 库，`.obsidian/` 里放的是库配置与**每个人的
# 窗口布局**（`workspace.json` 每次打开都变）。收进来只会让每次 `git status` 都脏一次，
# 与仓库要管的事实（文档本身）无关。文件仍在原地，只是不进库。
docs/.obsidian/

# 宿主侧分配器压测台（`crates/alloc-probe`）的本地 CARGO_HOME：`run.sh` 把依赖拉到这里
# （沙箱里 `~/.cargo/registry` 只读），内容是 cargo registry 缓存 + 源码，1.5 MB 级。
# 它是**缓存**不是源码：换台机器跑 `run.sh` 会自动重新拉。
.cargo-home/
# 那几台 crate 是**独立工作区**（被根 workspace `exclude`），故各自有一份 target——
# 根上的 `/target` 只盖根目录那一份。**照实记**：`line-case` 那一行是补的——它第一次提交时
# 漏了（114 个构建产物被带进索引），当场发现后从索引撤下、就地重做那一笔。
crates/alloc-probe/target/
# 编外的宿主靶（`crates/protocol-case` 那八台靶场、`crates/gate` 那一台门）：都是**不进工作区**
# 的独立 crate，各自有自己的 `target/`。**照实记**：新加那两台时漏了这两行，`git add -A` 把
# 703 个构建产物带进了索引（与上面那一次"114 个"同一条）——故这里改成一条**通配**，以后新开
# 一台不必再记着加（`crates/gate` 因此不必单独写一行）：
crates/*-case/target/
crates/gate/target/

# `scripts/teeth.py` 是一个 Python 量具：跑它会在旁边落一份字节码缓存。
__pycache__/
